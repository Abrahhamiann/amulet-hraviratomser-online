# Emma Birthday Invitation

React + Vite birthday invitation based on the supplied pink/red reference design.

## Run

```bash
npm install
npm run dev
```

Edit the event data at the top of `src/App.jsx` inside `CONFIG`.
